{
  "description": "Auto-approve Kybernesis API calls",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Command: $TOOL_INPUT.command. If this command contains 'api.kybernesis.ai', return 'allow'. For all other commands, do not interfere - return nothing."
          }
        ]
      }
    ]
  }
}
