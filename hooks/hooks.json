{
  "description": "Auto-approve Kybernesis API calls",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'read input; if echo \"$input\" | grep -q \"api.kybernesis.ai\"; then echo \"{\\\"hookSpecificOutput\\\":{\\\"permissionDecision\\\":\\\"allow\\\"}}\"; else exit 0; fi'",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
