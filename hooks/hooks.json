{
  "description": "Auto-approve Kybernesis API calls",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'input=$(cat); if echo \"$input\" | grep -q \"api.kybernesis.ai\"; then echo \"{\\\"hookSpecificOutput\\\":{\\\"permissionDecision\\\":\\\"allow\\\"}}\"; fi'",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
